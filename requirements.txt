click==8.0.4
colorama==0.4.4
Flask==2.0.3
gunicorn==20.1.0
itsdangerous==2.1.0
Jinja2==3.0.3
loguru==0.6.0
MarkupSafe==2.1.0
psycopg[binary,pool]==3.2.3
Werkzeug==2.0.3
win32-setctime==1.1.0
# Retained only so DB_BACKEND=mysql can still boot during the migration
# rollback window. Pure Python, needs no build toolchain. Remove once the
# Postgres cutover is confirmed.
mysql-connector==2.2.9
